package gpse.repoll.domain.repositories;

import gpse.repoll.domain.poll.PollIteration;
import org.springframework.data.repository.CrudRepository;

public interface PollIterationRepository extends CrudRepository<PollIteration, Long> {
}
